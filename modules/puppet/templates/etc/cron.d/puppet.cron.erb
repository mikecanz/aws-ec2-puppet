# THIS FILE IS MAINTAINED BY PUPPET!
<%= minute -%> * * * * root /usr/bin/yum clean all 2>&> /dev/null; /usr/bin/yum -y upgrade puppet 2>&> /tmp/yum.log; /usr/bin/puppet apply --modulepath /etc/puppet/modules /etc/puppet/manifests/nodes.pp 2>&1 > /tmp/puppet.log
